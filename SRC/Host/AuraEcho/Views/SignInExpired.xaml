<UserControl x:Class="AuraEcho.Views.SignInExpired"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="#60000000"
             Foreground="#EFEFEF"
             mc:Ignorable="d">
    <Border Width="320"
            Height="180"
            CornerRadius="4"
            Padding="8"
            Background="#212121">
        <Grid RowDefinitions="Auto, *, Auto">
            <Grid Grid.Row="0"
                  Height="24">
                <TextBlock Margin="4"
                           VerticalAlignment="Center"
                           Text="{Binding Source={StaticResource R}, Path=Labels.SignInExpired_Title}" />
            </Grid>
            <TextBlock Grid.Row="1"
                       Margin="0,0,0,0"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Source={StaticResource R}, Path=Labels.SignInExpired_Message}"
                       TextWrapping="Wrap" />
            <StackPanel Grid.Row="2"
                        Margin="8"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <Button Padding="24,8"
                        FontSize="16"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Command="{Binding SignInCommand}"
                        Content="{Binding Source={StaticResource R}, Path=Labels.SignInExpired_SignInButton}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
